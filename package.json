{
  "dependencies": {
    "@tailwindcss/cli": "^4.1.10",
    "bcryptjs": "^3.0.2",
    "cloudinary": "^2.7.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.0",
    "multer": "^2.0.1",
    "nodemon": "^3.1.10"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  },
  "name": "sheytracker",
  "version": "1.0.0",
  "main": "tailwind.config.js",
  "scripts": {
    "build": "cd server && npm install  && cd ../client && npm install && npm run build",
    "start": "node server/server.js",
    "dev": "NODE_OPTIONS=\"--openssl-legacy-provider\" nodemon server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
